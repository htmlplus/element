import { Context } from '../types/index.js';

export const validate = () => {

    const name = 'validate';

    const next = (context: Context) => {

    }

    return {
        name,
        next,
    }
}