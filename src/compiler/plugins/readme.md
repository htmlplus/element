# custom element

```js
read()
parse()
validate()
extract({
    prefix // TODO
})

// Optional
autoDependencyResolver({
    style,
    component
})

// Optional
sass()
scss()
styl()

// For custom element
customElement({
    engine: 'uhtml',
    prefix // TODO
})

// Transpile `.tsx` to `react`
react()
```
