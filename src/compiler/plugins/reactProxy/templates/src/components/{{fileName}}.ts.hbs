/* eslint-disable */
/* tslint:disable */
/* auto-generated */

import { proxy } from '../proxy';
import type { JSX } from '{{options.corePackageName}}';

{{#each components}}
import '{{options.corePackageName}}/{{fileName}}';
{{/each}}

{{#each components}}
const {{className}} = /*@__PURE__*/ proxy<{{elementInterfaceName}}, JSX.{{className}}>(
  '{{componentTag}}', 
  [
    {{#each classProperties}}
    '{{key.name}}',
    {{/each}}
  ], 
  [
    {{#each classEvents}}
    '{{key.name}}',
    {{/each}}
  ],
);

{{/each}}
{{#if single}}
export { {{root.className}} }
{{else}}
const All = /*@__PURE__*/ Object.assign({{root.className}}, {
  {{#each components}}
  {{className}}: {{className}},
  {{/each}}
});

export { All as {{root.className}} }
{{/if}}