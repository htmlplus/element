/* eslint-disable */
/* tslint:disable */

import { proxy } from '../proxy';
import type { JSX } from '{{corePackageName}}';
import '{{corePackageName}}/{{componentKey}}';

export const {{className}} = /*@__PURE__*/ proxy<{{elementInterfaceName}}, JSX.{{className}}>(
  '{{componentTag}}', 
  [
    {{#each classProperties}}
    '{{this.key.name}}',
    {{/each}}
  ], 
  [
    {{#each classEvents}}
    '{{this.key.name}}',
    {{/each}}
  ],
);