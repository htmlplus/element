/* eslint-disable */
/* tslint:disable */
/* auto-generated */

import { proxy } from '../proxy';

{{#each all}}
import { {{componentVariableRoot}} as {{componentVariableRootLocal}} } from '{{componentSource}}';
import type { {{componentTypeVariableRoot}} as {{componentTypeVariableRootLocal}} } from '{{componentTypeSource}}';

{{/each}}
{{#each all}}
const {{componentClassName}} = /*@__PURE__*/ proxy<{{componentInterfaceName}}, {{componentTypeVariable}}>(
  '{{componentTag}}', 
  [{{#each classProperties}}'{{key.name}}', {{/each}}], 
  [{{#each classEvents}}'{{key.name}}', {{/each}}],
);

{{/each}}
{{#if single}}
export { {{root.componentClassName}} }
{{else}}
const All = /*@__PURE__*/ Object.assign({{root.componentClassName}}, {
  {{#each filterd}}
  {{componentClassNameInCategory}}: {{componentClassName}},
  {{/each}}
});

export { All as {{root.componentClassName}} }
{{/if}}